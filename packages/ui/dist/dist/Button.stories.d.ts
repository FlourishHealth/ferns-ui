export function Primary(args: any): React.CElement<import("./Common").ButtonProps, Button>;
export namespace Primary {
    namespace args {
        const size: string;
        const color: string;
        const text: string;
        const type: string;
        const inline: boolean;
        const disabled: boolean;
    }
}
import { Button } from "./Button";
import React from "react";
