{
  "name": "ferns",
  "private": true,
  "version": "0.1.0",
  "description": "Fullstack Express React Native framework for building iOS, Android, and Web apps with an Express API",
  "license": "Apache-2.0",
  "scripts": {
    "web": "yarn --cwd packages/demo web",
    "ios": "yarn --cwd packages/demo ios",
    "android": "yarn --cwd packages/demo android",
    "lint": "yarn --cwd packages/demo lint && yarn --cwd packages/ui lint",
    "lintfix": "yarn --cwd packages/demo lintfix && yarn --cwd packages/ui lintfix",
    "build": "yarn --cwd packages/ui build",
    "preview": "yarn build && yarn --cwd packages/demo build-web",
    "clean": "rm -rf node_modules && rm -rf packages/ui/node_modules && rm -rf packages/demo/node_modules && yarn",
    "test": "yarn --cwd packages/ui test"
  },
  "workspaces": {
    "packages": [
      "app/*",
      "packages/*"
    ]
  }
}
